% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/html_table.R
\name{build_datatable_html}
\alias{build_datatable_html}
\title{Build a DataTables-Compatible HTML Table}
\usage{
build_datatable_html(
  data,
  col_spec,
  tbl_id = NULL,
  tbl_attr = list(),
  header_wrap = NULL,
  indent = 0L,
  replace_na = ""
)
}
\arguments{
\item{data}{A data frame containing the table data.}

\item{col_spec}{A named list defining each table column. Each name becomes
the \verb{<th>} header text. Each element is a list with:
\itemize{
\item \code{content}: name of the column in \code{data} used as \verb{<td>} content (may be
pre-rendered HTML).
\item \code{search}: \emph{(optional)} name of the column in \code{data} used as the
\code{data-search} attribute value. \code{NA} values are always omitted (the
\code{data-search} attribute is dropped entirely), regardless of
\code{replace_na}.
\item \code{td_attr}: \emph{(optional)} named list of additional static attributes
applied to every \verb{<td>} in the column.
}}

\item{tbl_id}{The \code{id} attribute for the \verb{<table>} element, as a string.}

\item{tbl_attr}{A named list of additional attributes for the \verb{<table>}
element (e.g. \code{list(style = "width:100\%;", class = "display")}).}

\item{header_wrap}{An optional length-2 character vector \code{c(prefix, suffix)}
wrapping each \verb{<th>} text. Useful for template engine syntax such as
Babel/Jinja2, e.g. \code{c('{{ _("', '") }}')}.}

\item{indent}{Either an integer giving the starting indent level for the
\verb{<table>} tag (each level adds two spaces; defaults to \code{0L}), or an
\code{html_indent} object as returned by \code{\link[=get_html_indent]{get_html_indent()}} to match the
indentation style of an existing HTML file.}

\item{replace_na}{A string to replace \code{NA} values with, or \code{NULL} to leave
them as-is.}
}
\value{
A single character string containing the complete table HTML,
with newline-separated lines and consistent indentation.
}
\description{
Generates a complete \verb{<table>} HTML string suitable for use with the
\href{https://datatables.net/}{DataTables} JavaScript library. Each column is
described via \code{col_spec}, which maps display headers to data columns and
optionally to a \code{data-search} attribute for per-cell searching.
}

# scopuslite contains functions to manage scopus info from csv files (not API)

# combine_scopus_csv <- function(path, id) {
#
#     dirs <- tools::file_test(path)
#     if (stringr::str_detect(path, "csv$")
#     if (missing(id) {
#
#
#     citations_as_list <- purrr::map(files, readr::read_csv) %>%
